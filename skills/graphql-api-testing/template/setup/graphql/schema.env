# GraphQL schema (SDL) location for the GraphQL API testing skill.
#
# Used by:
# - $CODEX_HOME/skills/graphql-api-testing/scripts/gql-schema.sh
#
# Notes:
# - This file is safe to commit (no secrets).
# - For per-developer overrides, create:
#     setup/graphql/schema.local.env
#   (and keep it uncommitted / gitignored via setup/graphql/*.local.env).
# - Relative paths are resolved under setup/graphql/.

# Canonical, committed schema file path (recommended: keep it under setup/graphql/)
GQL_SCHEMA_FILE=schema.gql

