# Fixture: Contract not first H2

## Overview

This file intentionally violates the rule that `## Contract` must be the first H2.

## Contract

Prereqs:

- `bash`

Inputs:

- `--file <path>`

Outputs:

- Validation output on stdout/stderr

Exit codes:

- `0`: success
- `1`: failure

Failure modes:

- Contract is not first H2
