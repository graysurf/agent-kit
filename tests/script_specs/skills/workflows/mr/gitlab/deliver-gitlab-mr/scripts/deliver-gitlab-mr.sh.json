{
  "args": ["--help"],
  "timeout_sec": 5,
  "expect": {
    "exit_codes": [0],
    "stdout_regex": "deliver-gitlab-mr\\.sh --kind <feature\\|bug\\|config\\|deploy\\|docs\\|chore> <command>"
  },
  "smoke": [
    {
      "name": "help-surface",
      "args": ["--help"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "wait-pipeline"
      }
    }
  ]
}
