{
  "args": ["--help"],
  "timeout_sec": 5,
  "expect": {
    "exit_codes": [0],
    "stdout_regex": "close-gitlab-mr\\.sh --kind <feature\\|bug\\|config\\|deploy\\|docs\\|chore>"
  },
  "smoke": [
    {
      "name": "help-surface",
      "args": ["--help"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "close-gitlab-mr\\.sh --kind <feature\\|bug\\|config\\|deploy\\|docs\\|chore>"
      }
    }
  ]
}
