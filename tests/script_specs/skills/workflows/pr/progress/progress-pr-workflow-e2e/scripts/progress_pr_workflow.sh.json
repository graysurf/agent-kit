{
  "smoke": [
    {
      "name": "refuse-in-ci",
      "args": ["--phase", "init"],
      "env": {
        "CI": "true",
        "E2E_ALLOW_REAL_GH": null
      },
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [1],
        "stderr_regex": "(?m)^error: refusing to run in CI=true$"
      }
    }
  ]
}
