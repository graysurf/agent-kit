{
  "args": ["--help"],
  "timeout_sec": 5,
  "expect": {
    "exit_codes": [0],
    "stdout_regex": "close-github-pr\\.sh --kind <feature\\|bug>"
  },
  "smoke": [
    {
      "name": "help-surface",
      "args": ["--help"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "close-github-pr\\.sh --kind <feature\\|bug>"
      }
    }
  ]
}
