{
  "args": ["--help"],
  "timeout_sec": 5,
  "expect": {
    "exit_codes": [0],
    "stdout_regex": "deliver-github-pr\\.sh --kind <feature\\|bug> <command>"
  },
  "smoke": [
    {
      "name": "help-surface",
      "args": ["--help"],
      "timeout_sec": 5,
      "expect": {
        "exit_codes": [0],
        "stdout_regex": "deliver-github-pr\\.sh --kind <feature\\|bug> <command>"
      }
    }
  ]
}
