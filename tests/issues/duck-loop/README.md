# Duck Loop Root (Disposable Test Fixture)

This folder is test-only and disposable.
It exists only for plan-issue-delivery-loop validation.

Do not treat anything under `tests/issues/duck-loop` as production or long-lived project content.
See `tests/issues/duck-loop/CLEANUP.md` for the one-command removal step.
